
<div class="row">
  <div class="col-lg-2 col-sm-12">
    <%= link_to 'Edit', edit_image_path(@image) %>
    |
    <%= link_to 'Delete', @image, method: :delete, data: { confirm: 'Are you sure?' } %>
    <br/>
    Uploader: <%= link_to(@image.user, @image.user) %><br/>
    Tags:<br/>
    <% @image.image_tags.each do |image_tag| %>
      <span class="label label-info">
        <%= link_to image_tag.tag, image_tag.tag %>
        <%= link_to image_tag, method: :delete, data: { confirm: 'Are you sure?' } do %>
          <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
        <% end %>
      </span>
      <br/>
    <% end %>
    <br/>
    <%= form_for @image_tag do |f| %>
      <%= f.hidden_field :image_id, :value => @image.id %>
      <%= f.select(:tag_id, options_from_collection_for_select(@tags, :id, :to_s), {}, { class:  'form-control'} )%>
      <%= f.submit value:"add tag" %>
    <% end %>
  </div>
  <div class="col-lg-8 col-sm-12">
    <%= image_tag @image.picture.url(:medium) %>
  </div>
</div>